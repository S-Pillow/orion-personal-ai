name: orion-vault-actions
version: "0.1.0"
description: "Orion Phase 5 source-only native vault preview and approval-contract plugin. P5-01 does not perform protected filesystem mutation."
hooks:
  - pre_tool_call
provides_tools:
  - orion_vault_preview_edit
  - orion_vault_preview_move_draft
  - orion_vault_apply_plan
