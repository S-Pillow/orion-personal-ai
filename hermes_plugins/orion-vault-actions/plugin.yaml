name: orion-vault-actions
version: "0.2.0"
description: "Orion Phase 5 guarded vault preview and apply plugin. Protected mutation remains disabled unless explicit mutation_enabled mode passes the qualified approval and recovery guardrails."
provides_hooks:
  - pre_tool_call
  - post_approval_response
provides_tools:
  - orion_vault_preview_edit
  - orion_vault_preview_move_draft
  - orion_vault_recommend_destination
  - orion_vault_apply_plan
