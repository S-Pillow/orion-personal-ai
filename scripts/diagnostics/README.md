# Diagnostics

Only reusable diagnostics that have passed cleanly should be promoted into this directory.

Known-broken intermediate harness revisions should remain out of the canonical scripts tree unless retained elsewhere for a specific forensic reason.

Current plan:

- add a clean gateway service-materialization diagnostic only after final polishing
- add a gateway lifecycle-transition diagnostic after the three-snapshot start/stop experiment is accepted
